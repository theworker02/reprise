{
  "name": "@reprise/recovery",
  "version": "1.1.0-rc.1",
  "description": "Checkpoint reconstruction, recovery guard, and receipts for Reprise",
  "license": "UNLICENSED",
  "private": true,
  "type": "module",
  "exports": { ".": { "types": "./dist/index.d.ts", "import": "./dist/index.js" } },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@reprise/core": "workspace:*",
    "@reprise/graph": "workspace:*",
    "@reprise/ledger": "workspace:*",
    "@reprise/compatibility": "workspace:*",
    "@reprise/policy": "workspace:*",
    "@reprise/verifier": "workspace:*"
  },
  "devDependencies": { "@types/node": "^22.10.2", "typescript": "^5.7.2", "vitest": "^2.1.8" }
}

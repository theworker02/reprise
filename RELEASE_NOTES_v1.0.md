# Reprise v1.0 release notes draft

Release status: **not ready for v1.0.0**.

The repository is at `0.9.0` and contains a verified recovery reasoning vertical slice. It lacks a live Neon adapter, persistent checkpoint indexing, agent server, dashboard, complete CI/security/SBOM gates, and a clean-install validation result. It must not be tagged as v1.0.0 until those items are implemented and independently validated.

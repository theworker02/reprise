{
  "name": "@reprise/cli",
  "version": "1.1.0-rc.3",
  "description": "Safe recovery CLI for Reprise",
  "license": "UNLICENSED",
  "private": true,
  "type": "module",
  "bin": { "reprise": "./dist/index.js" },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@reprise/core": "workspace:*",
    "@reprise/ledger": "workspace:*",
    "@reprise/policy": "workspace:*",
    "@reprise/recovery": "workspace:*",
    "@reprise/verifier": "workspace:*"
  },
  "devDependencies": { "@types/node": "^22.10.2", "typescript": "^5.7.2", "vitest": "^2.1.8" }
}

# CLI

```text
reprise demo acquisition [--json]
```

Runs the local v1.7 → v1.8 incompatible-function incident fixture. It reconstructs the v1.7 compound checkpoint, runs six verification checks, evaluates the Recovery Guard with promotion disabled, and emits a receipt ID. It has no external network or provider dependency.

Production mutation commands are deliberately not available yet.
